function 






























flattenArray(originalArray){
var arr=originalArray.slice();
var flat=[];
while(arr.length){
var last=arr.pop();
if(Array.isArray(last)){
Array.prototype.push.apply(arr,last);}else 
{
flat.push(last);}}


return flat.reverse();}


module.exports=flattenArray;